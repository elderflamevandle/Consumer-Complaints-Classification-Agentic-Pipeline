---
phase: 05-streamlit-hitl-dashboard
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/streamlit_app.py
  - src/ui/runtime.py
  - src/ui/telemetry.py
  - src/llm/client.py
  - tests/test_ui_runtime.py
  - tests/test_streamlit_app.py
autonomous: true
requirements:
  - UI-02
  - UI-04
  - UI-05
must_haves:
  truths:
    - "The dashboard runtime returns ordered stage snapshots with status, model, latency, and token usage."
    - "The main control room uses compact stage cards by default and reveals deeper stage artifacts through expanders."
    - "A dedicated audit tab shows ordered thread decision events with timestamps, decisions, and model versions from SQLite."
    - "A compact header widget shows daily token-budget usage and warning/degrade state."
    - "Completed runs surface the final customer response and explainer artifacts in the dashboard instead of hiding them behind logs only."
    - "Manual complaints and loaded golden demos execute through the same composer-submit path into the dashboard runtime."
  artifacts:
    - path: "src/ui/runtime.py"
      provides: "UI-safe complaint runtime facade that executes the pipeline and returns dashboard snapshots"
      min_lines: 120
    - path: "src/ui/telemetry.py"
      provides: "Typed stage telemetry and budget/audit snapshot contracts"
      contains: "latency_ms"
    - path: "src/llm/client.py"
      provides: "Shared client-side response metadata needed for dashboard token/model telemetry"
      contains: "total_tokens"
    - path: "app/streamlit_app.py"
      provides: "Compact telemetry cards with expanders, dedicated audit tab with timestamp/decision/model fields, budget widget, and final response/explainer rendering"
      contains: "st.tabs"
    - path: "tests/test_ui_runtime.py"
      provides: "Pipeline runtime telemetry contract coverage"
      min_lines: 80
    - path: "tests/test_streamlit_app.py"
      provides: "AppTest coverage for stage cards, audit tab, and budget widget"
      min_lines: 110
  key_links:
    - from: "src/ui/runtime.py"
      to: "src/tools/audit_logger.py"
      via: "thread-scoped audit decision loading for the dedicated audit tab"
      pattern: "fetch_events|thread_id|decision"
    - from: "src/ui/runtime.py"
      to: "src/llm/client.py"
      via: "stage snapshot capture of model, token, and fallback metadata"
      pattern: "total_tokens|model|attempts|degraded"
    - from: "src/ui/runtime.py"
      to: "src/llm/rate_limiter.py"
      via: "budget snapshot helper wired to the existing daily token tracker"
      pattern: "snapshot|used_tokens|warning|degrade_non_critical"
    - from: "app/streamlit_app.py"
      to: "src/ui/telemetry.py"
      via: "compact stage-card rendering with expanders plus audit and budget surfaces"
      pattern: "model|latency|tokens|status|timestamp"
    - from: "app/streamlit_app.py"
      to: "src/ui/runtime.py"
      via: "shared complaint-form submission for both manual text and loaded demos"
      pattern: "submit|complaint_text|selected_demo|run_complaint"
    - from: "app/streamlit_app.py"
      to: "src/ui/runtime.py"
      via: "rendering of final response and explainer artifacts from completed dashboard snapshots"
      pattern: "final_response|final_explanation|response_loop_status"
---

<objective>
Implement the Phase 5 runtime, telemetry surfaces, audit tab, and budget widget.

Purpose: expose the existing complaint pipeline as a demo-ready control room with operational visibility instead of a black-box submit/result page.
Output: runtime facade + telemetry contracts + audit-tab rendering + budget widget + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-streamlit-hitl-dashboard/05-CONTEXT.md
@.planning/phases/05-streamlit-hitl-dashboard/05-RESEARCH.md
@.planning/phases/05-streamlit-hitl-dashboard/05-VALIDATION.md
@.planning/phases/05-streamlit-hitl-dashboard/05-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed stage telemetry contracts and capture hooks for pipeline runs</name>
  <files>src/ui/telemetry.py, src/llm/client.py, tests/test_ui_runtime.py</files>
  <behavior>
    - Each pipeline stage exposes machine-readable status, model, latency, and token-usage data.
    - Telemetry capture does not change the underlying business logic contracts or routing decisions.
    - The resulting snapshot shape is stable enough for Streamlit rendering and pytest assertions.
  </behavior>
  <action>Introduce typed telemetry objects, add shared response metadata capture where the LLM client already centralizes completion results, and write the first runtime telemetry tests in the same task so later stages can verify against a real contract.</action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_runtime.py::test_pipeline_runtime_returns_stage_telemetry</automated>
  </verify>
  <done>The pipeline exposes real stage-level telemetry suitable for UI rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dashboard runtime facade plus audit and budget snapshot helpers</name>
  <files>src/ui/runtime.py, tests/test_ui_runtime.py</files>
  <behavior>
    - Runs the existing complaint pipeline end-to-end and returns a dashboard snapshot keyed by thread id.
    - Loads chronological audit decision events for the current thread from SQLite.
    - Exposes the daily token-budget snapshot through a UI-safe helper contract.
    - Preserves timestamp, decision, and model-version fields needed by the audit tab, not just generic event ordering.
    - Carries final response and explanation artifacts forward so the dashboard can show user-visible outputs after a successful run.
  </behavior>
  <action>Create a dedicated runtime facade that composes the existing intake, routing, diagnosis, remediation, response, and explainability logic into a single UI-safe API for the Streamlit app, and explicitly carry ordered audit decision fields, budget data, final response output, and explanation artifacts for downstream runtime tests.</action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_runtime.py::test_runtime_snapshot_exposes_budget_audit_and_final_outputs</automated>
  </verify>
  <done>The dashboard has one runtime surface for complaint execution, audit lookup, and budget display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render stage telemetry cards, chronological audit tab, and header budget widget</name>
  <files>app/streamlit_app.py, tests/test_ui_runtime.py, tests/test_streamlit_app.py</files>
  <behavior>
    - Main stage cards stay compact by default, show model/latency/token/status, and reveal deeper artifacts through expanders.
    - Audit detail is separated into its own tab and ordered oldest-to-newest for the active thread with visible timestamps, decision labels, and model versions.
    - Daily token budget remains visible in a compact header widget throughout the run.
    - Completed runs render the final customer response and concise explanation artifacts in the control room.
    - Manual complaint text and a loaded golden demo both execute through the same composer-submit path into the runtime facade.
  </behavior>
  <action>Connect the runtime facade to the Streamlit control room so users can submit either manual text or a loaded golden demo through the same composer form, then see compact stage cards, chronological audit decision rows, budget state, and final response/explanation outputs without leaving the single-workspace dashboard.</action>
  <verify>
    <automated>uv run pytest -q tests/test_streamlit_app.py::test_manual_and_demo_inputs_execute_through_shared_submit_path tests/test_streamlit_app.py::test_stage_cards_show_model_latency_and_tokens tests/test_streamlit_app.py::test_stage_cards_use_expanders_for_deeper_detail tests/test_streamlit_app.py::test_audit_tab_renders_timestamp_decision_and_model_fields_in_order tests/test_streamlit_app.py::test_dashboard_shows_budget_widget_and_status_summary tests/test_streamlit_app.py::test_dashboard_renders_final_response_and_explainer_artifacts</automated>
  </verify>
  <done>UI-02, UI-04, and UI-05 are visible and regression tested in the dashboard, including the shared app-to-runtime submit path, explicit audit decision fields, budget widget, and final response/explainer output surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_ui_runtime.py tests/test_streamlit_app.py` passes
- [ ] Stage cards display real model, latency, token, and status values from runtime telemetry
- [ ] Stage cards remain compact by default and reveal deeper detail through expanders
- [ ] Audit tab shows chronological thread decision events with timestamp, decision, and model fields, and the budget widget reflects the tracker snapshot
- [ ] Completed runs display final response and explanation artifacts in the dashboard
- [ ] Manual complaints and loaded demos execute through the same composer-submit path into the runtime facade
</verification>

<success_criteria>

- All tasks completed
- UI-02 implemented with stage status, model choice, latency, and token visibility
- UI-04 implemented with a dedicated audit tab backed by SQLite events
- UI-05 implemented with an always-visible daily budget widget
</success_criteria>

<output>
After completion, create `.planning/phases/05-streamlit-hitl-dashboard/05-02-SUMMARY.md`
</output>

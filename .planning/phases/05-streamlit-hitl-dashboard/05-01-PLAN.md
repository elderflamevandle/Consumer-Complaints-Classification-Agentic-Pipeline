---
phase: 05-streamlit-hitl-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - scripts/tasks.py
  - app/streamlit_app.py
  - src/ui/__init__.py
  - src/ui/dashboard_state.py
  - src/ui/demo_cases.py
  - data/golden_demos.json
  - tests/test_ui_runtime.py
  - tests/test_streamlit_app.py
autonomous: true
requirements:
  - DATA-03
  - UI-01
must_haves:
  truths:
    - "The Streamlit dashboard opens into a single control-room shell with a blank complaint composer."
    - "The system includes five curated golden demo complaints that are visible as scenario cards and load on demand."
    - "Golden demo selection and free-text entry share the same editable composer and the same submission path."
    - "The landing shell keeps scenario-card loading and complaint submission controls adjacent so the shared run path is obvious to an operator."
  artifacts:
    - path: "app/streamlit_app.py"
      provides: "Phase 5 Streamlit control-room shell with composer and scenario-card loading behavior"
      contains: "st.form"
    - path: "data/golden_demos.json"
      provides: "Five curated golden demo complaints for dashboard loading"
      min_lines: 20
    - path: "src/ui/dashboard_state.py"
      provides: "Typed dashboard session state and complaint-source contract"
      contains: "active_thread_id"
    - path: "tests/test_streamlit_app.py"
      provides: "AppTest coverage for landing view and shared composer behavior"
      min_lines: 70
  key_links:
    - from: "src/ui/demo_cases.py"
      to: "data/golden_demos.json"
      via: "typed scenario-card loading and stable demo identifiers"
      pattern: "title|summary|complaint_text"
    - from: "app/streamlit_app.py"
      to: "src/ui/dashboard_state.py"
      via: "session-backed control-room rendering and composer state"
      pattern: "session_state|complaint_text|selected_demo"
    - from: "tests/test_streamlit_app.py"
      to: "app/streamlit_app.py"
      via: "landing view and demo-card-to-composer assertions"
      pattern: "composer|demo|scenario"
---

<objective>
Implement the Phase 5 control-room shell, shared complaint composer, and golden demo loading contract.

Purpose: establish the first operator-facing Streamlit surface so free-text complaints and curated demo cases can be run from one clear workspace.
Output: Streamlit app shell + typed dashboard session state + five golden demos + AppTest regression coverage.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-streamlit-hitl-dashboard/05-CONTEXT.md
@.planning/phases/05-streamlit-hitl-dashboard/05-RESEARCH.md
@.planning/phases/05-streamlit-hitl-dashboard/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed dashboard session state and curated golden demo catalog</name>
  <files>src/ui/__init__.py, src/ui/dashboard_state.py, src/ui/demo_cases.py, data/golden_demos.json, tests/test_ui_runtime.py</files>
  <behavior>
    - Defines typed dashboard session state for complaint draft, selected demo, active thread id, and last dashboard snapshot.
    - Loads exactly five curated scenario records with stable ids, titles, summaries, and complaint text.
    - Keeps golden demos separate from rendering logic so later runtime and review work can reuse the same source.
    - Creates the first runtime test coverage for the golden-demo catalog in the same task as the catalog contract.
  </behavior>
  <action>Create the initial `src/ui` module with typed session/demo models, a five-case golden demo dataset that the Streamlit shell can render as scenario cards, and the first runtime test that proves the catalog contract.</action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_runtime.py::test_demo_catalog_contains_five_curated_cases</automated>
  </verify>
  <done>Phase 5 has a reusable golden-demo catalog and dashboard state contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Streamlit control-room shell with shared editable composer</name>
  <files>pyproject.toml, scripts/tasks.py, app/streamlit_app.py, tests/test_streamlit_app.py</files>
  <behavior>
    - Adds the Streamlit dependency and a repo-level command path to run the dashboard.
    - Renders a blank complaint composer on load with visible scenario cards nearby.
    - Loads demo complaints into the same editable composer instead of branching into a separate mode or page.
    - Establishes initial AppTest coverage for the control-room landing view in the same task as the shell implementation.
  </behavior>
  <action>Implement the initial Streamlit app shell and task-runner entrypoint, and create the first AppTest landing assertions so the dashboard is runnable and verifiable from the moment the shell exists.</action>
  <verify>
    <automated>uv run pytest -q tests/test_streamlit_app.py::test_dashboard_landing_view_shows_composer_and_demo_cards</automated>
  </verify>
  <done>The app opens into the intended control-room shell with a shared complaint path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add AppTest coverage for landing view and demo loading path</name>
  <files>tests/test_ui_runtime.py, tests/test_streamlit_app.py, app/streamlit_app.py</files>
  <behavior>
    - Tests verify five golden demos exist and render as scenario cards.
    - Tests verify selecting a demo loads its complaint into the same editable composer used for manual entry.
    - Tests verify the initial app shell can be exercised through AppTest without manual browser interaction.
  </behavior>
  <action>Write focused runtime and Streamlit AppTest coverage for the new app shell so later telemetry and review work builds on a stable operator-entry contract.</action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_runtime.py tests/test_streamlit_app.py</automated>
  </verify>
  <done>The Phase 5 shell and demo-loading behavior are regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_ui_runtime.py tests/test_streamlit_app.py` passes
- [ ] The dashboard shows a blank complaint composer and five visible scenario cards on first load
- [ ] Golden demo selection loads into the same editable composer used for manual complaints
</verification>

<success_criteria>

- All tasks completed
- DATA-03 satisfied with five curated loadable demo complaints
- UI-01 satisfied for raw complaint input and golden demo selection
- The control-room shell exists and is regression-tested
</success_criteria>

<output>
After completion, create `.planning/phases/05-streamlit-hitl-dashboard/05-01-SUMMARY.md`
</output>

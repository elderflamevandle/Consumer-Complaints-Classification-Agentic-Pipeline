---
phase: 05-streamlit-hitl-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - app/streamlit_app.py
  - src/ui/dashboard_state.py
  - src/ui/review.py
  - src/ui/runtime.py
  - tests/test_review_session.py
  - tests/test_streamlit_app.py
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "A sticky review panel exposes approve, edit, and reject actions without leaving the control-room workspace."
    - "The sticky review panel keeps complaint summary, interrupt reason, and current stage output visible while the reviewer acts."
    - "Inline edits reuse the same active thread and do not force a fresh complaint run."
    - "Submitting a reviewer action auto-resumes the same thread and gives explicit status feedback."
    - "Reject flow remains visible in the dashboard and preserves thread history for audit review."
  artifacts:
    - path: "src/ui/review.py"
      provides: "Review-panel helpers and typed review-edit adapter logic"
      contains: "approve"
    - path: "src/ui/dashboard_state.py"
      provides: "Session-backed active-thread and review-panel continuity contract"
      contains: "pending_review"
    - path: "app/streamlit_app.py"
      provides: "Sticky review panel and auto-resume dashboard flow"
      contains: "approve|edit|reject"
    - path: "tests/test_review_session.py"
      provides: "Same-thread review continuity and reviewer-action regression coverage"
      min_lines: 80
  key_links:
    - from: "src/ui/runtime.py"
      to: "src/graph/interrupts.py"
      via: "same-thread reviewer-action handoff from UI to existing interrupt contract"
      pattern: "apply_reviewer_action|thread_id"
    - from: "src/ui/review.py"
      to: "src/ui/runtime.py"
      via: "focused review context rendering from the active interrupt payload and stage snapshot"
      pattern: "summary|reason|stage_output"
    - from: "app/streamlit_app.py"
      to: "src/ui/dashboard_state.py"
      via: "sticky review panel state persisted across reruns"
      pattern: "active_thread_id|pending_review|banner"
    - from: "tests/test_review_session.py"
      to: "src/ui/review.py"
      via: "approve/edit/reject and same-thread continuity assertions"
      pattern: "resume|reject|edit"
---

<objective>
Implement inline HITL review controls and same-thread resume continuity for the Phase 5 dashboard.

Purpose: let a reviewer approve, edit, or reject interrupted runs without leaving the control room or restarting the active thread.
Output: sticky review panel + review/session helpers + same-thread resume behavior + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-streamlit-hitl-dashboard/05-CONTEXT.md
@.planning/phases/05-streamlit-hitl-dashboard/05-RESEARCH.md
@.planning/phases/05-streamlit-hitl-dashboard/05-VALIDATION.md
@.planning/phases/05-streamlit-hitl-dashboard/05-01-PLAN.md
@.planning/phases/05-streamlit-hitl-dashboard/05-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed review-session continuity and interrupt-adapter helpers</name>
  <files>src/ui/dashboard_state.py, src/ui/review.py, src/ui/runtime.py, tests/test_review_session.py</files>
  <behavior>
    - Stores active thread id, pending review payload, draft edits, and banner state across reruns.
    - Maps the dashboard review surface onto the existing reviewer-action contract.
    - Keeps resume logic explicit so approve, edit, and reject can be tested independently.
  </behavior>
  <action>Extend the dashboard session and review helper layer so the Streamlit app can preserve review state across reruns, dispatch same-thread reviewer actions deterministically, and establish the first review-session tests before panel rendering begins.</action>
  <verify>
    <automated>uv run pytest -q tests/test_review_session.py::test_interrupt_panel_exposes_reviewer_actions</automated>
  </verify>
  <done>The review panel has a typed state and action contract that survives Streamlit reruns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sticky review panel with inline edit and auto-resume flow</name>
  <files>app/streamlit_app.py, src/ui/review.py, src/ui/runtime.py</files>
  <behavior>
    - Renders a persistent review surface inside the control room when a run pauses for human action.
    - Supports inline edit, approve, and reject without leaving the current workspace.
    - Keeps complaint summary, interrupt reason, and current stage output visible next to the action controls.
    - Auto-resumes the same thread after reviewer submission and shows explicit status feedback.
  </behavior>
  <action>Build the sticky review panel and same-thread resume UX so the dashboard fulfills the Phase 5 HITL requirement without switching pages or re-running the complaint from scratch, and wire the focused review context directly from the runtime payload.</action>
  <verify>
    <automated>uv run pytest -q tests/test_review_session.py::test_review_edit_resumes_same_thread</automated>
  </verify>
  <done>The dashboard supports inline HITL actions with deterministic same-thread continuation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add review continuity regression coverage for approve, edit, and reject flows</name>
  <files>tests/test_review_session.py, tests/test_streamlit_app.py, app/streamlit_app.py</files>
  <behavior>
    - Tests verify the sticky review panel exposes all required actions and focused context.
    - Tests verify edit and approve flows preserve thread identity and resume status correctly.
    - Tests verify reject flow remains visible in the dashboard and preserves auditable thread history.
  </behavior>
  <action>Write focused Streamlit and runtime tests for same-thread review continuity so the control room cannot regress into restart-style behavior during later UI changes.</action>
  <verify>
    <automated>uv run pytest -q tests/test_review_session.py tests/test_streamlit_app.py</automated>
  </verify>
  <done>UI-03 review continuity is regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_review_session.py tests/test_streamlit_app.py` passes
- [ ] Approve, edit, and reject actions preserve the active thread instead of restarting the run
- [ ] The dashboard shows explicit post-action feedback and keeps rejected history visible for audit review
</verification>

<success_criteria>

- All tasks completed
- UI-03 implemented with inline approve/edit/reject controls
- Same-thread resume continuity is explicit and test covered
- Reviewer actions remain visible and auditable within the control-room workspace
</success_criteria>

<output>
After completion, create `.planning/phases/05-streamlit-hitl-dashboard/05-03-SUMMARY.md`
</output>

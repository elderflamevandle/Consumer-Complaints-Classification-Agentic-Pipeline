---
phase: 06-evaluation-fairness-and-robustness
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/evaluation/fairness.py
  - src/evaluation/harness.py
  - src/evaluation/reporting.py
  - scripts/evaluate_classifier.py
  - tests/test_fairness_report.py
  - tests/test_evaluation_harness.py
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - "Fairness reporting computes disparity ratios across a fixed shortlist of product-level complaint groups."
    - "The fairness report uses one explicit named baseline group and keeps that choice stable across runs."
    - "Groups below the support threshold are skipped with visible warnings rather than being silently scored."
    - "Fairness results are surfaced in the saved evaluation artifacts and command output without creating a new offline-metrics dashboard view."
  artifacts:
    - path: "src/evaluation/fairness.py"
      provides: "Selected-group fairness computation and disparity-ratio policy"
      contains: "baseline"
    - path: "src/evaluation/reporting.py"
      provides: "Markdown and JSON fairness sections with support counts and warnings"
      min_lines: 80
    - path: "tests/test_fairness_report.py"
      provides: "Regression coverage for baseline selection, support thresholds, and disparity output"
      min_lines: 70
  key_links:
    - from: "src/evaluation/fairness.py"
      to: "src/evaluation/harness.py"
      via: "normalized evaluation rows feeding selected-group disparity comparisons"
      pattern: "group|baseline|support|ratio"
    - from: "src/evaluation/reporting.py"
      to: "scripts/evaluate_classifier.py"
      via: "artifact rendering and CLI summary output"
      pattern: "fairness|warning|baseline"
    - from: "tests/test_fairness_report.py"
      to: "src/evaluation/fairness.py"
      via: "named-baseline and underpowered-group assertions"
      pattern: "skip|baseline|ratio"
---

<objective>
Implement Phase 6 fairness computation and report-first surfacing for selected complaint groups.

Purpose: turn the user's fairness decisions into a deterministic offline report that compares supported product groups without expanding the dashboard beyond the existing live-run surface.
Output: fairness computation module + fairness sections in evaluation artifacts + regression coverage.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-CONTEXT.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-RESEARCH.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-VALIDATION.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the fixed product shortlist, baseline policy, and support-threshold rules</name>
  <files>src/evaluation/fairness.py, tests/test_fairness_report.py</files>
  <behavior>
    - Selects a stable product-group shortlist from the normalized evaluation space.
    - Uses one explicit named baseline group for all disparity comparisons.
    - Skips groups below the support threshold and emits explicit warnings describing why they were omitted.
  </behavior>
  <action>Create the fairness policy layer so Phase 6 can compare supported complaint groups consistently instead of choosing groups ad hoc on each run.</action>
  <verify>
    <automated>uv run pytest -q tests/test_fairness_report.py::test_selected_groups_use_named_baseline_and_skip_underpowered_groups</automated>
  </verify>
  <done>The fairness comparison policy is explicit, stable, and regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend evaluation artifacts with disparity ratios, support counts, and warnings</name>
  <files>src/evaluation/fairness.py, src/evaluation/harness.py, src/evaluation/reporting.py, scripts/evaluate_classifier.py, tests/test_fairness_report.py, tests/test_evaluation_harness.py</files>
  <behavior>
    - Computes fairness ratios from the normalized evaluation results produced by Plan 06-01.
    - Surfaces support counts, baseline choice, and skipped-group warnings in both JSON and Markdown outputs.
    - Keeps fairness reporting in the offline evaluation artifact path rather than adding a new dashboard view.
  </behavior>
  <action>Wire fairness computation into the holdout harness and report renderer so Phase 6 can produce a concise, reviewable fairness section alongside the main metrics.</action>
  <verify>
    <automated>uv run pytest -q tests/test_fairness_report.py::test_fairness_report_includes_ratios_support_and_warnings</automated>
  </verify>
  <done>Fairness results are computed and surfaced in deterministic evaluation artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add end-to-end fairness regression coverage on the evaluation path</name>
  <files>tests/test_fairness_report.py, tests/test_evaluation_harness.py, scripts/evaluate_classifier.py</files>
  <behavior>
    - Tests verify the evaluation runner carries fairness results through to the final artifact set.
    - Tests verify the baseline group is named explicitly and remains stable across repeat runs.
    - Tests verify skipped groups are warned about instead of failing silently or producing misleading ratios.
  </behavior>
  <action>Write focused integration coverage for the fairness path so the report contract remains trustworthy as later demo-hardening work lands.</action>
  <verify>
    <automated>uv run pytest -q tests/test_evaluation_harness.py tests/test_fairness_report.py</automated>
  </verify>
  <done>Phase 6 fairness reporting is end-to-end and regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_evaluation_harness.py tests/test_fairness_report.py` passes
- [ ] Fairness artifacts show a named baseline, support counts, and skipped-group warnings
- [ ] No new offline-metrics dashboard surface is introduced just to display fairness output
</verification>

<success_criteria>

- All tasks completed
- EVAL-02 satisfied with deterministic disparity reporting for selected complaint groups
- Fairness output is report-first, stable, and interpretable
- Underpowered groups are handled explicitly instead of being hidden
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-fairness-and-robustness/06-02-SUMMARY.md`
</output>

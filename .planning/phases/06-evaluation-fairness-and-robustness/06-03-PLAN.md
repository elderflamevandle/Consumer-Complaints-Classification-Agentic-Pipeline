---
phase: 06-evaluation-fairness-and-robustness
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/intake/pipeline.py
  - src/ui/runtime.py
  - app/streamlit_app.py
  - tests/test_intake_pipeline.py
  - tests/test_ui_runtime.py
  - tests/test_streamlit_app.py
  - tests/test_phase6_reliability.py
  - tests/test_llm_client.py
  - tests/test_routing_interrupts.py
autonomous: true
requirements:
  - FLOW-04
  - OPS-02
must_haves:
  truths:
    - "Empty complaints, ambiguous complaints, and very long complaints do not crash the pipeline."
    - "Ambiguous or low-confidence inputs continue to respect the existing same-thread review-routing contract."
    - "Non-critical fallback or degraded runs remain successful when possible, but they are visible through existing telemetry or audit surfaces."
    - "Targeted automated tests cover fallback behavior, schema validation, and core graph transitions relevant to Phase 6 hardening."
  artifacts:
    - path: "src/intake/pipeline.py"
      provides: "FLOW-04 edge-case intake contract for empty, ambiguous, and long complaints"
      contains: "review_policy"
    - path: "src/ui/runtime.py"
      provides: "Runtime warning propagation for degraded or fallback paths"
      min_lines: 120
    - path: "tests/test_phase6_reliability.py"
      provides: "Targeted Phase 6 reliability regressions for hardening behavior"
      min_lines: 70
    - path: "tests/test_llm_client.py"
      provides: "Existing fallback and degrade-policy regressions extended for Phase 6 requirements"
      min_lines: 80
  key_links:
    - from: "src/intake/pipeline.py"
      to: "src/graph/routing.py"
      via: "edge-case intake output feeding deterministic review decisions"
      pattern: "route_to_human_review|warning_continue|human_review_required"
    - from: "src/ui/runtime.py"
      to: "app/streamlit_app.py"
      via: "warning visibility through existing telemetry surfaces instead of a new control flow"
      pattern: "warning|degrade|fallback"
    - from: "tests/test_phase6_reliability.py"
      to: "tests/test_llm_client.py"
      via: "Phase 6 reliability coverage reusing fallback-policy guarantees"
      pattern: "fallback|degraded|route"
---

<objective>
Implement Phase 6 edge-case hardening and targeted reliability coverage for the existing pipeline.

Purpose: improve demo reliability without changing the project's routing model by formalizing edge-case behavior and surfacing non-critical fallback warnings through the current runtime and UI patterns.
Output: hardened intake/runtime behavior + visible fallback warnings + focused reliability regression suite.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-CONTEXT.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-RESEARCH.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-VALIDATION.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formalize empty, ambiguous, and long-input handling in intake/runtime boundaries</name>
  <files>src/intake/pipeline.py, tests/test_intake_pipeline.py, tests/test_phase6_reliability.py</files>
  <behavior>
    - Empty complaints return a deterministic non-crashing path with explicit warnings or error status.
    - Very long complaints are handled safely and predictably without blowing up downstream stages.
    - Ambiguous inputs preserve the existing low-confidence review-routing semantics from earlier phases.
  </behavior>
  <action>Tighten the edge-case contract at the intake boundary so Phase 6 reliability claims are anchored in production code rather than only in report language.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase6_reliability.py::test_prepare_intake_handles_empty_and_very_long_inputs_without_crash</automated>
  </verify>
  <done>FLOW-04 edge-case behavior is explicit and regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface non-critical fallback and degraded-path warnings through existing runtime/UI patterns</name>
  <files>src/ui/runtime.py, app/streamlit_app.py, tests/test_ui_runtime.py, tests/test_streamlit_app.py, tests/test_phase6_reliability.py</files>
  <behavior>
    - Successful degraded or fallback runs remain non-blocking.
    - Runtime snapshots expose clear warning signals using the existing telemetry or audit model.
    - The Streamlit control room reuses current stage surfaces for visibility instead of introducing a new pause mode or offline metrics page.
  </behavior>
  <action>Extend the runtime and existing dashboard surfaces just enough to make fallback and degraded behavior obvious to an operator without changing the control-room model established in Phase 5.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase6_reliability.py::test_runtime_surfaces_non_critical_fallback_warning_without_failing_run</automated>
  </verify>
  <done>Operators can see non-critical fallback behavior without losing run continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add targeted regression coverage for fallback behavior and core graph transitions</name>
  <files>tests/test_phase6_reliability.py, tests/test_llm_client.py, tests/test_routing_interrupts.py, tests/test_intake_pipeline.py</files>
  <behavior>
    - Tests verify fallback and degrade behavior remain deterministic and visible.
    - Tests verify core review-routing transitions still preserve same-thread continuity.
    - Tests verify the hardened intake path does not regress prior routing and warning behavior.
  </behavior>
  <action>Expand the existing regression suites with Phase 6-focused reliability checks so fallback routing, schema-safety, and critical transitions stay protected after hardening changes.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase6_reliability.py tests/test_llm_client.py tests/test_routing_interrupts.py</automated>
  </verify>
  <done>Phase 6 reliability behavior is covered by targeted automated regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_phase6_reliability.py tests/test_llm_client.py tests/test_routing_interrupts.py` passes
- [ ] Empty and very long complaints do not crash the pipeline
- [ ] Non-critical fallback behavior is visible without turning successful runs into failures
- [ ] Same-thread review routing remains intact for ambiguous or risky complaints
</verification>

<success_criteria>

- All tasks completed
- FLOW-04 satisfied with deterministic edge-case handling
- OPS-02 satisfied with targeted automated coverage for fallback behavior and core graph transitions
- Reliability hardening improves demo stability without changing the established review-routing model
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-fairness-and-robustness/06-03-SUMMARY.md`
</output>

---
phase: 06-evaluation-fairness-and-robustness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/tasks.py
  - scripts/evaluate_classifier.py
  - src/evaluation/__init__.py
  - src/evaluation/taxonomy.py
  - src/evaluation/harness.py
  - src/evaluation/reporting.py
  - tests/test_evaluation_taxonomy.py
  - tests/test_evaluation_harness.py
  - tests/test_task_runner.py
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "The repo-level `eval` task runs a real holdout evaluation over `data/processed/holdout.parquet` instead of a placeholder print."
    - "Ground-truth labels are normalized from raw CFPB product and issue strings into the project's classifier enums before any metric is computed."
    - "Evaluation artifacts include macro F1 plus a per-class breakdown derived from the normalized holdout run."
    - "The evaluation report captures a small deterministic sample of failures so metric regressions are reviewable."
  artifacts:
    - path: "src/evaluation/taxonomy.py"
      provides: "Deterministic raw-label normalization contract for holdout truth labels"
      contains: "normalize"
    - path: "src/evaluation/harness.py"
      provides: "Batch holdout evaluation runner over the existing classifier"
      min_lines: 90
    - path: "scripts/evaluate_classifier.py"
      provides: "CLI entrypoint for Phase 6 holdout evaluation"
      min_lines: 50
    - path: "tests/test_evaluation_harness.py"
      provides: "Regression coverage for macro F1, per-class breakdown, and sampled failures"
      min_lines: 70
  key_links:
    - from: "src/evaluation/taxonomy.py"
      to: "src/schemas/classification.py"
      via: "raw CFPB labels normalized into project enum taxonomy before scoring"
      pattern: "ProductType|IssueType|OTHER"
    - from: "src/evaluation/harness.py"
      to: "src/agents/classifier.py"
      via: "batch classifier execution and normalized metric collection"
      pattern: "classify|confidence|used_fallback"
    - from: "scripts/tasks.py"
      to: "scripts/evaluate_classifier.py"
      via: "repo-level eval command dispatch"
      pattern: "eval"
---

<objective>
Implement the Phase 6 holdout evaluation harness and raw-label normalization contract.

Purpose: make evaluation repeatable and credible by turning the existing holdout split and classifier contract into a real repo task with deterministic artifacts.
Output: production taxonomy adapter + batch evaluation harness + real `eval` command + regression coverage.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-CONTEXT.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-RESEARCH.md
@.planning/phases/06-evaluation-fairness-and-robustness/06-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add production raw-label normalization for holdout truth labels</name>
  <files>src/evaluation/__init__.py, src/evaluation/taxonomy.py, tests/test_evaluation_taxonomy.py</files>
  <behavior>
    - Converts raw CFPB `product` and `issue` strings into the project's classifier enum space deterministically.
    - Makes unsupported or ambiguous raw labels explicit `OTHER` rather than relying on ad hoc fallthrough during scoring.
    - Encodes the mapping in reusable source code, not notebook-only logic.
  </behavior>
  <action>Create the Phase 6 taxonomy adapter and its first regression tests so the evaluation harness can compare predictions against valid ground truth instead of incompatible raw labels.</action>
  <verify>
    <automated>uv run pytest -q tests/test_evaluation_taxonomy.py</automated>
  </verify>
  <done>Holdout truth labels can be normalized into the classifier contract reliably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic holdout evaluation harness and artifact writer</name>
  <files>src/evaluation/harness.py, src/evaluation/reporting.py, scripts/evaluate_classifier.py, tests/test_evaluation_harness.py</files>
  <behavior>
    - Loads `data/processed/holdout.parquet` and runs the existing classifier across the dataset.
    - Computes macro F1 plus per-class breakdown from normalized truth labels and predicted labels.
    - Captures a small deterministic sample of failures for review and writes JSON plus Markdown artifacts.
  </behavior>
  <action>Build the offline evaluation runner and reporting layer so Phase 6 can produce repeatable quality evidence from the shipped holdout split.</action>
  <verify>
    <automated>uv run pytest -q tests/test_evaluation_harness.py::test_holdout_report_computes_macro_f1_and_class_breakdown</automated>
  </verify>
  <done>The project can produce deterministic holdout metrics and readable artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the eval task stub and lock the command/report contract with tests</name>
  <files>scripts/tasks.py, scripts/evaluate_classifier.py, tests/test_evaluation_taxonomy.py, tests/test_evaluation_harness.py, tests/test_task_runner.py</files>
  <behavior>
    - Replaces the current placeholder `eval` task with the real evaluation command path.
    - Keeps the repo's task-runner-first workflow intact for teammates and demo preparation.
    - Regression-tests both the taxonomy contract and the repo command surface.
  </behavior>
  <action>Wire the new evaluation harness into the task runner and add the task-runner assertions needed so the Phase 6 evaluation path stays discoverable and stable.</action>
  <verify>
    <automated>uv run pytest -q tests/test_evaluation_taxonomy.py tests/test_evaluation_harness.py tests/test_task_runner.py::test_task_runner_resolves_expected_commands</automated>
  </verify>
  <done>Phase 6 evaluation is reachable through the repo-standard `eval` task and protected by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_evaluation_taxonomy.py tests/test_evaluation_harness.py tests/test_task_runner.py::test_task_runner_resolves_expected_commands` passes
- [ ] `uv run python scripts/tasks.py eval` performs a real holdout evaluation
- [ ] Evaluation artifacts include macro F1, per-class breakdown, and sampled failure rows
</verification>

<success_criteria>

- All tasks completed
- EVAL-01 satisfied with a real holdout evaluation harness
- Raw CFPB labels are normalized into the classifier contract before scoring
- The repo exposes the evaluation harness through the standard task runner
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-fairness-and-robustness/06-01-SUMMARY.md`
</output>

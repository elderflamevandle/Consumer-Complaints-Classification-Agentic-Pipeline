---
phase: 04-response-generation-with-compliance-audit-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/response.py
  - src/agents/writer.py
  - tests/test_response_writer.py
autonomous: true
requirements:
  - AGT-05
must_haves:
  truths:
    - "Response writer generates a customer-facing draft with a clear resolution statement and safe tone."
    - "Draft output preserves fixed 4-block structure: acknowledgment, findings, action steps, timeline/next steps."
    - "Draft exposes explicit policy citation labels when remediation citations are available."
    - "Guardrails prevent liability admissions and guaranteed outcomes."
  artifacts:
    - path: "src/schemas/response.py"
      provides: "Typed response-draft contract and render-friendly section schema"
      contains: "resolution_statement"
    - path: "src/agents/writer.py"
      provides: "AGT-05 writer flow with fixed structure and post-parse guardrails"
      min_lines: 90
    - path: "tests/test_response_writer.py"
      provides: "AGT-05 structure, citation, and safe-tone regression tests"
      min_lines: 70
  key_links:
    - from: "src/agents/writer.py"
      to: "src/schemas/response.py"
      via: "schema-validated draft output before rendering"
      pattern: "resolution_statement|acknowledgment|timeline"
    - from: "tests/test_response_writer.py"
      to: "src/agents/writer.py"
      via: "customer draft structure and guardrail assertions"
      pattern: "policy|tone|guarantee|liability"
---

<objective>
Implement AGT-05 customer response generation contract for Phase 4.

Purpose: ensure the pipeline can produce a customer-ready draft that is structured, policy-grounded, and safe enough to hand to the auditor loop.
Output: response schema + writer agent + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-CONTEXT.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-RESEARCH.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema-validated customer response draft contract</name>
  <files>src/schemas/response.py</files>
  <behavior>
    - Defines structured response payload with resolution statement and fixed 4-block content fields.
    - Supports explicit policy citation labels and safe render helpers.
    - Rejects extra fields so downstream audit behavior stays deterministic.
  </behavior>
  <action>Create pydantic models for the writer output and related section data so Phase 4 drafts are machine-checkable before they are rendered into customer-facing text.</action>
  <verify>
    <automated>uv run pytest -q tests/test_response_writer.py::test_writer_generates_four_block_response</automated>
  </verify>
  <done>Writer output has a strict, reusable schema contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement writer agent with policy labels and response guardrails</name>
  <files>src/agents/writer.py</files>
  <behavior>
    - Consumes scrubbed complaint context plus classification, diagnosis, and remediation artifacts.
    - Returns a schema-valid draft with calm professional tone and clear resolution statement.
    - Applies explicit checks that block guarantees and liability admissions in generated text.
  </behavior>
  <action>Implement the response writer using the shared LLM client, fixed response structure, and post-parse safety checks so the output is both customer-ready and auditable.</action>
  <verify>
    <automated>uv run pytest -q tests/test_response_writer.py::test_writer_includes_resolution_statement_and_policy_labels</automated>
  </verify>
  <done>AGT-05 writer behavior is implemented with deterministic structure and policy labeling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add AGT-05 regression coverage for structure and tone safety</name>
  <files>tests/test_response_writer.py</files>
  <behavior>
    - Tests verify the 4-block draft structure and required resolution statement.
    - Tests verify policy citation labels are preserved in the rendered draft.
    - Tests verify banned overcommitment language is rejected or sanitized.
  </behavior>
  <action>Write focused tests for structure, citation visibility, and commitment guardrails so the writer contract stays stable during later loop integration work.</action>
  <verify>
    <automated>uv run pytest -q tests/test_response_writer.py</automated>
  </verify>
  <done>AGT-05 response drafting is regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_response_writer.py` passes
- [ ] Writer output always includes a clear resolution statement and fixed 4-block structure
- [ ] Generated text does not admit liability or guarantee outcomes
</verification>

<success_criteria>

- All tasks completed
- AGT-05 implemented with schema-first customer response drafting
- Writer output is policy-labeled, calm-professional, and safe for audit review
- Tests protect structure, citation visibility, and commitment guardrails
</success_criteria>

<output>
After completion, create `.planning/phases/04-response-generation-with-compliance-audit-loop/04-01-SUMMARY.md`
</output>

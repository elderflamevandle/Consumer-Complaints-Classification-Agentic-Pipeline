---
phase: 04-response-generation-with-compliance-audit-loop
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/schemas/auditor.py
  - src/agents/auditor.py
  - src/graph/state.py
  - src/graph/response_loop.py
  - tests/test_auditor_loop.py
autonomous: true
requirements:
  - AGT-06
must_haves:
  truths:
    - "Compliance auditor evaluates the response draft and returns PASS or FAIL with explicit reason codes."
    - "Failed audits route back to the response writer with must-fix critique context."
    - "Rewrite loop is capped at 2 retries, then escalates to human review."
    - "Auditor checks policy grounding, compliance safety, and customer-safe tone together."
  artifacts:
    - path: "src/schemas/auditor.py"
      provides: "Typed auditor verdict, fail-code, and critique-item contracts"
      contains: "PASS"
    - path: "src/agents/auditor.py"
      provides: "AGT-06 review flow with coded verdicts and rewrite recommendations"
      min_lines: 90
    - path: "src/graph/response_loop.py"
      provides: "Deterministic writer-auditor cyclic routing with retry cap and escalation"
      min_lines: 80
    - path: "tests/test_auditor_loop.py"
      provides: "AGT-06 verdict, routing, and retry-cap regression tests"
      min_lines: 70
  key_links:
    - from: "src/graph/response_loop.py"
      to: "src/agents/auditor.py"
      via: "verdict-driven rewrite or continue routing"
      pattern: "PASS|FAIL|human_review|rewrite"
    - from: "tests/test_auditor_loop.py"
      to: "src/graph/state.py"
      via: "rewrite count and critique handoff assertions"
      pattern: "rewrite|reason_code|must_fix"
---

<objective>
Implement AGT-06 compliance auditing and deterministic rewrite-loop routing for Phase 4.

Purpose: ensure customer drafts are reviewed against explicit compliance criteria and automatically rewritten only within a bounded, inspectable loop.
Output: auditor schema + auditor agent + cyclic routing helper + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-CONTEXT.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-RESEARCH.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-VALIDATION.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed auditor verdict and critique contracts</name>
  <files>src/schemas/auditor.py</files>
  <behavior>
    - Defines verdict schema with PASS/FAIL, reason codes, critique summary, and must-fix items.
    - Keeps fail reasons machine-readable and stable across rewrites.
    - Provides a clean contract for routing and explainability to consume.
  </behavior>
  <action>Create pydantic models and reason-code enums for auditor outputs so Phase 4 loop behavior can route on deterministic verdict data instead of prose parsing.</action>
  <verify>
    <automated>uv run pytest -q tests/test_auditor_loop.py::test_auditor_returns_reason_coded_fail</automated>
  </verify>
  <done>Auditor outputs have a strict verdict contract suitable for loop control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auditor agent and capped writer-auditor rewrite routing</name>
  <files>src/agents/auditor.py, src/graph/state.py, src/graph/response_loop.py</files>
  <behavior>
    - Auditor evaluates writer drafts for policy grounding, tone safety, and structure completeness.
    - FAIL verdict sends coded critique back to writer while retries remain.
    - After 2 rewrites, unresolved failures escalate to human review deterministically.
  </behavior>
  <action>Implement the auditor and a dedicated response-loop helper that tracks rewrite count, applies verdict routing, and preserves same-thread continuity for subsequent state and logging work.</action>
  <verify>
    <automated>uv run pytest -q tests/test_auditor_loop.py::test_fail_routes_back_to_writer_until_retry_cap</automated>
  </verify>
  <done>AGT-06 rewrite-loop behavior is available and bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add AGT-06 regression coverage for verdicts, routing, and retry cap</name>
  <files>tests/test_auditor_loop.py</files>
  <behavior>
    - Tests verify reason-coded FAIL verdicts and PASS continuation behavior.
    - Tests verify same-thread rewrite routing and escalation after cap.
    - Tests verify writer and auditor node outcomes are logged consistently.
  </behavior>
  <action>Write focused tests for failed-audit rewrites, pass-through success, and retry-cap escalation so the Phase 4 control loop cannot regress silently.</action>
  <verify>
    <automated>uv run pytest -q tests/test_auditor_loop.py</automated>
  </verify>
  <done>AGT-06 auditing and cyclic routing are regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_auditor_loop.py` passes
- [ ] Auditor returns PASS/FAIL plus explicit reason codes on every review
- [ ] Failed drafts rewrite at most 2 times before escalating to human review
</verification>

<success_criteria>

- All tasks completed
- AGT-06 implemented with coded auditor verdicts and deterministic rewrite routing
- Retry cap and escalation behavior are explicit and test-covered
- Writer and auditor loop behavior remains same-thread and auditable
</success_criteria>

<output>
After completion, create `.planning/phases/04-response-generation-with-compliance-audit-loop/04-02-SUMMARY.md`
</output>

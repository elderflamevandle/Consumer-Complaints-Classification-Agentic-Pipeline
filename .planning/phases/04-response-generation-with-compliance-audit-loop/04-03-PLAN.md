---
phase: 04-response-generation-with-compliance-audit-loop
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/schemas/explainer.py
  - src/agents/explainer.py
  - src/graph/state.py
  - tests/test_explainer_agent.py
  - tests/test_phase4_state.py
autonomous: true
requirements:
  - AGT-07
  - FLOW-03
must_haves:
  truths:
    - "Graph state stores bounded rewrite memory so auditor critiques influence later response drafts."
    - "Persisted response-loop memory includes latest draft, unresolved issues, rewrite count, and per-cycle trace."
    - "Stored loop memory remains scrubbed-safe and avoids unbounded transcript accumulation."
    - "Explainer produces a concise stage-chain rationale covering classification, diagnosis, remediation, final response, and audit outcome."
  artifacts:
    - path: "src/graph/state.py"
      provides: "FLOW-03 bounded response-loop memory contract and cycle-trace fields"
      contains: "unresolved_issues"
    - path: "src/schemas/explainer.py"
      provides: "Typed stage-chain explanation schema with deterministic bullet ordering"
      min_lines: 50
    - path: "src/agents/explainer.py"
      provides: "AGT-07 explanation generator grounded in prior stage artifacts"
      min_lines: 90
    - path: "tests/test_phase4_state.py"
      provides: "FLOW-03 critique-memory regression tests"
      min_lines: 60
    - path: "tests/test_explainer_agent.py"
      provides: "AGT-07 stage-chain explanation regression tests"
      min_lines: 60
  key_links:
    - from: "src/graph/state.py"
      to: "src/agents/writer.py"
      via: "unresolved critique items shaping rewrites"
      pattern: "unresolved|rewrite|latest_response"
    - from: "src/agents/explainer.py"
      to: "src/agents/auditor.py"
      via: "final verdict and critique trace included in rationale chain"
      pattern: "verdict|reason_code|critique"
---

<objective>
Implement FLOW-03 bounded rewrite memory and AGT-07 final explainability output for Phase 4.

Purpose: preserve actionable critique across rewrites and expose a concise rationale chain that matches the actual pipeline decisions made in prior stages.
Output: state extensions + explainer schema/agent + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-CONTEXT.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-RESEARCH.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-VALIDATION.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-01-PLAN.md
@.planning/phases/04-response-generation-with-compliance-audit-loop/04-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend graph state with bounded critique-memory and cycle trace fields</name>
  <files>src/graph/state.py</files>
  <behavior>
    - Persists latest response draft, unresolved critique items, rewrite count, and compact cycle trace.
    - Preserves scrubbed-safe summaries only and avoids full transcript accumulation.
    - Prioritizes unresolved must-fix items before newest critique when preparing rewrite context.
  </behavior>
  <action>Finalize Phase 4 state extensions and helper behavior so auditor feedback survives rewrites in a compact, deterministic form that remains safe for downstream explainability.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase4_state.py::test_response_loop_state_carries_unresolved_issues_across_rewrites</automated>
  </verify>
  <done>FLOW-03 bounded memory contract is established in graph state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explainer schema and AGT-07 stage-chain generator</name>
  <files>src/schemas/explainer.py, src/agents/explainer.py</files>
  <behavior>
    - Produces 5-7 bullets in deterministic stage order.
    - Covers classification, diagnosis, remediation, final response, and audit outcome.
    - Uses selective citations from prior structured artifacts rather than raw complaint text.
  </behavior>
  <action>Implement the explainer over existing stage outputs and final auditor verdict so reviewers can see a concise rationale chain that matches the executed pipeline.</action>
  <verify>
    <automated>uv run pytest -q tests/test_explainer_agent.py::test_explainer_outputs_stage_chain_bullets</automated>
  </verify>
  <done>AGT-07 explanation output is implemented with deterministic structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add FLOW-03 and AGT-07 regression coverage</name>
  <files>tests/test_phase4_state.py, tests/test_explainer_agent.py</files>
  <behavior>
    - Tests verify unresolved issues persist across rewrites and per-cycle trace is retained.
    - Tests verify explainer output stays within target bullet count and stage ordering.
    - Tests verify explanation is grounded in final artifacts instead of raw intake text.
  </behavior>
  <action>Write focused tests for critique-memory continuity and final rationale-chain quality so Phase 4 remains explainable after later UI and reliability work.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase4_state.py tests/test_explainer_agent.py</automated>
  </verify>
  <done>FLOW-03 memory behavior and AGT-07 explainability are regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_phase4_state.py tests/test_explainer_agent.py` passes
- [ ] Rewrite context carries unresolved critique items and cycle metadata across retries
- [ ] Explainer output is deterministic, concise, and grounded in final pipeline artifacts
</verification>

<success_criteria>

- All tasks completed
- FLOW-03 implemented with bounded critique-memory and cycle trace support
- AGT-07 implemented with deterministic stage-chain explanation output
- Tests protect rewrite-state continuity and explanation grounding
</success_criteria>

<output>
After completion, create `.planning/phases/04-response-generation-with-compliance-audit-loop/04-03-SUMMARY.md`
</output>


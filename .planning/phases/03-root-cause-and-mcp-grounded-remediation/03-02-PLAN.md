---
phase: 03-root-cause-and-mcp-grounded-remediation
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - mcp_server/__init__.py
  - mcp_server/server.py
  - mcp_server/mock_regulations.json
  - src/tools/mcp_policy_client.py
  - src/agents/remediator.py
  - tests/test_remediator_mcp.py
autonomous: true
requirements:
  - AGT-04
must_haves:
  truths:
    - "Remediator calls get_sla_requirements(issue_type, state_code) before producing action plan."
    - "MCP responses include sla_window, required_actions, and regulatory_basis."
    - "MCP failure or invalid payload yields POLICY_UNAVAILABLE and human-review route."
    - "Remediation output is ordered and policy-cited."
  artifacts:
    - path: "mcp_server/server.py"
      provides: "FastMCP policy tool endpoint exposing get_sla_requirements"
      contains: "get_sla_requirements"
    - path: "src/tools/mcp_policy_client.py"
      provides: "MCP client wrapper with required-field validation"
      min_lines: 70
    - path: "src/agents/remediator.py"
      provides: "AGT-04 remediator flow with mandatory policy grounding"
      min_lines: 90
    - path: "tests/test_remediator_mcp.py"
      provides: "Mandatory-call, failure-policy, and citation regression tests"
      min_lines: 60
  key_links:
    - from: "src/agents/remediator.py"
      to: "src/tools/mcp_policy_client.py"
      via: "pre-action MCP lookup gate"
      pattern: "get_sla_requirements|POLICY_UNAVAILABLE"
    - from: "src/tools/mcp_policy_client.py"
      to: "mcp_server/server.py"
      via: "tool discovery and invocation"
      pattern: "tool|sla|required_actions|regulatory_basis"
---

<objective>
Implement AGT-04 policy-grounded remediation with a real MCP boundary.

Purpose: ensure recommendations are explicitly grounded in SLA/regulatory guidance and fail safely when policy data is unavailable.
Output: FastMCP server module + MCP client adapter + remediator agent + tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-CONTEXT.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-RESEARCH.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-VALIDATION.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FastMCP policy server contract</name>
  <files>mcp_server/__init__.py, mcp_server/server.py, mcp_server/mock_regulations.json</files>
  <behavior>
    - Exposes `get_sla_requirements(issue_type, state_code)` tool.
    - Returns deterministic payload including `sla_window`, `required_actions`, `regulatory_basis`.
    - Supports local startup for test and demo use.
  </behavior>
  <action>Implement local MCP policy server and mock regulation dataset for common issue/state combinations used in demos and tests.</action>
  <verify>
    <automated>uv run pytest -q tests/test_remediator_mcp.py::test_mcp_tool_is_called_before_action_plan</automated>
  </verify>
  <done>MCP policy server contract exists and is callable through standard tool interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MCP policy client wrapper with guardrails</name>
  <files>src/tools/mcp_policy_client.py</files>
  <behavior>
    - Initializes MCP client and resolves required tool.
    - Validates required policy fields in tool response.
    - Surfaces deterministic error state for unavailable/invalid policy payload.
  </behavior>
  <action>Create MCP client helper that encapsulates discovery, invocation, timeout/retry defaults, and normalized failure outputs consumed by remediator.</action>
  <verify>
    <automated>uv run pytest -q tests/test_remediator_mcp.py::test_policy_unavailable_routes_human_review</automated>
  </verify>
  <done>MCP integration behavior is deterministic and safe under tool failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement remediator agent with policy citations and AGT-04 tests</name>
  <files>src/agents/remediator.py, tests/test_remediator_mcp.py</files>
  <behavior>
    - Blocks remediation generation until MCP response is available and validated.
    - Produces ordered action plan tied to policy citations.
    - Emits `POLICY_UNAVAILABLE` + human-review route when policy tool fails.
  </behavior>
  <action>Implement remediator flow that combines classification/root-cause context with MCP policy payload, then add tests verifying mandatory MCP call ordering and output citation correctness.</action>
  <verify>
    <automated>uv run pytest -q tests/test_remediator_mcp.py</automated>
  </verify>
  <done>AGT-04 remediation contract is implemented and regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_remediator_mcp.py` passes
- [ ] No remediation action plan is produced without successful MCP policy retrieval
- [ ] POLICY_UNAVAILABLE path and human-review route are explicitly test-covered
</verification>

<success_criteria>

- All tasks completed
- AGT-04 implemented with true MCP server/client boundary
- Remediation outputs are policy-grounded and citation-backed
- Failure modes are deterministic and route-safe
</success_criteria>

<output>
After completion, create `.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-02-SUMMARY.md`
</output>

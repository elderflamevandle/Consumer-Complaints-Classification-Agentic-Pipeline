---
phase: 03-root-cause-and-mcp-grounded-remediation
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/tools/audit_logger.py
  - src/graph/state.py
  - src/graph/routing.py
  - src/graph/interrupts.py
  - src/agents/root_cause.py
  - src/agents/remediator.py
  - tests/test_audit_logger.py
  - tests/test_phase3_graph_logging.py
autonomous: true
requirements:
  - FLOW-05
must_haves:
  truths:
    - "Each active node outcome writes one structured audit event to SQLite."
    - "Audit event fields include thread_id, node, timestamp, model, latency_ms, and decision."
    - "Persisted complaint text is scrubbed-only; raw complaint text is excluded."
    - "SQLite write failure queues warning and allows pipeline continuation."
  artifacts:
    - path: "src/tools/audit_logger.py"
      provides: "Central SQLite logging utility and fail-open warning queue"
      min_lines: 90
    - path: "tests/test_audit_logger.py"
      provides: "FLOW-05 event-schema and failure-path regression tests"
      min_lines: 60
    - path: "tests/test_phase3_graph_logging.py"
      provides: "Cross-node event consistency checks for thread continuity"
      min_lines: 50
  key_links:
    - from: "src/agents/root_cause.py"
      to: "src/tools/audit_logger.py"
      via: "node outcome logging hook"
      pattern: "thread_id|latency|decision"
    - from: "src/agents/remediator.py"
      to: "src/tools/audit_logger.py"
      via: "policy-grounded remediation event logging"
      pattern: "model|decision|POLICY_UNAVAILABLE"
    - from: "tests/test_phase3_graph_logging.py"
      to: "src/graph/state.py"
      via: "thread-consistent event assertions"
      pattern: "thread_id|events|node"
---

<objective>
Implement FLOW-05 node-level audit logging across active Phase 3 graph nodes.

Purpose: provide consistent, scrubbed, and resilient audit persistence needed for explainability and demo credibility.
Output: centralized audit logger + node hooks + regression tests for schema and failure behavior.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-CONTEXT.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-RESEARCH.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-VALIDATION.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-01-PLAN.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build centralized SQLite audit logger with required event schema</name>
  <files>src/tools/audit_logger.py</files>
  <behavior>
    - Creates/uses a local SQLite table for node events.
    - Enforces required event fields and scrubbed-text persistence policy.
    - Supports warning-queue fallback when DB write fails.
  </behavior>
  <action>Implement logger utilities for schema setup, insert operations, and in-memory warning queue capture so pipeline remains available under transient DB failures.</action>
  <verify>
    <automated>uv run pytest -q tests/test_audit_logger.py::test_node_event_written_with_required_fields</automated>
  </verify>
  <done>FLOW-05 logging schema and fail-open primitives are available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire logger hooks into active node outcomes and routing transitions</name>
  <files>src/graph/state.py, src/graph/routing.py, src/graph/interrupts.py, src/agents/root_cause.py, src/agents/remediator.py</files>
  <behavior>
    - Classifier-routing, root-cause, remediator, and review transitions emit one event each.
    - Event payloads carry thread and decision metadata consistently.
    - Raw intake text is never logged.
  </behavior>
  <action>Add logging hook calls at node boundaries and ensure same-thread continuity in emitted events for downstream audit querying.</action>
  <verify>
    <automated>uv run pytest -q tests/test_phase3_graph_logging.py::test_phase3_nodes_emit_thread_consistent_events</automated>
  </verify>
  <done>Active Phase 3 path emits consistent structured audit events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add FLOW-05 regression coverage for schema and failure behavior</name>
  <files>tests/test_audit_logger.py, tests/test_phase3_graph_logging.py</files>
  <behavior>
    - Tests verify required fields and scrubbed-text-only persistence.
    - Tests verify SQLite failure continues pipeline with warning queue updates.
    - Tests verify thread consistency across logged node events.
  </behavior>
  <action>Write focused tests for successful writes, DB failure fallback, and cross-node event consistency for Phase 3 execution path.</action>
  <verify>
    <automated>uv run pytest -q tests/test_audit_logger.py tests/test_phase3_graph_logging.py</automated>
  </verify>
  <done>FLOW-05 behavior is regression protected for normal and degraded conditions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_audit_logger.py tests/test_phase3_graph_logging.py` passes
- [ ] Every active node emits exactly one structured event with required fields
- [ ] SQLite failure path preserves pipeline continuity and warning telemetry
</verification>

<success_criteria>

- All tasks completed
- FLOW-05 implemented with centralized logger and consistent node hooks
- Logged data remains scrubbed and auditable
- Failure behavior is deterministic and test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-03-SUMMARY.md`
</output>

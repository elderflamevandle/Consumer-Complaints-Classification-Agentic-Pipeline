---
phase: 03-root-cause-and-mcp-grounded-remediation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/root_cause.py
  - src/tools/vector_search.py
  - src/agents/root_cause.py
  - tests/test_root_cause_agent.py
autonomous: true
requirements:
  - AGT-03
must_haves:
  truths:
    - "Root-cause stage retrieves top-5 similar complaints and uses them as explicit evidence context."
    - "Diagnosis output includes one root-cause statement plus ranked supporting evidence."
    - "Each evidence item includes citation metadata: id, product, issue, and date."
    - "Conflicting evidence sets an explicit AMBIGUOUS flag."
  artifacts:
    - path: "src/tools/vector_search.py"
      provides: "Deterministic top-5 retrieval wrapper over seeded local vector artifacts"
      min_lines: 70
    - path: "src/schemas/root_cause.py"
      provides: "Typed diagnosis and evidence citation contracts"
      contains: "AMBIGUOUS"
    - path: "src/agents/root_cause.py"
      provides: "AGT-03 root-cause reasoning flow with ranked evidence output"
      min_lines: 80
    - path: "tests/test_root_cause_agent.py"
      provides: "AGT-03 retrieval/evidence/ambiguity regression tests"
      min_lines: 60
  key_links:
    - from: "src/agents/root_cause.py"
      to: "src/tools/vector_search.py"
      via: "retrieve top-5 context before diagnosis generation"
      pattern: "top[-_ ]?5|retrieve|similar"
    - from: "tests/test_root_cause_agent.py"
      to: "src/agents/root_cause.py"
      via: "schema and evidence citation assertions"
      pattern: "evidence|citation|ambiguous"
---

<objective>
Implement AGT-03 retrieval-grounded diagnosis contract for Phase 3.

Purpose: guarantee root-cause outputs are evidence-backed, explainable, and deterministic enough for audit review.
Output: retrieval utility + root-cause schemas/agent + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-CONTEXT.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-RESEARCH.md
@.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add root-cause output schemas and citation contracts</name>
  <files>src/schemas/root_cause.py</files>
  <behavior>
    - Defines structured root-cause payload with diagnosis, ranked evidence list, and ambiguity flag.
    - Evidence schema requires citation keys: id, product, issue, date.
    - Schema rejects extra fields to keep machine-consumable contract strict.
  </behavior>
  <action>Create pydantic models for diagnosis output and citation items, including explicit `AMBIGUOUS` representation and validators needed by downstream remediator and logger stages.</action>
  <verify>
    <automated>uv run pytest -q tests/test_root_cause_agent.py::test_root_cause_outputs_ranked_evidence_with_citations</automated>
  </verify>
  <done>Root-cause structured contract is established and enforceable in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic top-5 vector retrieval utility</name>
  <files>src/tools/vector_search.py</files>
  <behavior>
    - Loads existing seeded vector storage and returns top-5 similar complaints.
    - Preserves metadata fields required for evidence citations.
    - Applies deterministic ordering and tie-break behavior.
  </behavior>
  <action>Build retrieval wrapper around current local vector artifacts (`chroma_db` or fallback file) and expose a stable API consumed by root-cause agent.</action>
  <verify>
    <automated>uv run pytest -q tests/test_root_cause_agent.py::test_retrieves_top5_similar_cases</automated>
  </verify>
  <done>Retriever reliably returns evidence-ready top-5 complaint context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement root-cause agent behavior and AGT-03 tests</name>
  <files>src/agents/root_cause.py, tests/test_root_cause_agent.py</files>
  <behavior>
    - Uses shared Groq client for diagnosis generation.
    - Consumes retriever context and returns schema-valid output.
    - Sets `AMBIGUOUS` when evidence themes conflict.
  </behavior>
  <action>Implement root-cause agent orchestration and add focused tests for citation coverage, ranked evidence order, and ambiguity behavior under conflicting retrieval context.</action>
  <verify>
    <automated>uv run pytest -q tests/test_root_cause_agent.py</automated>
  </verify>
  <done>AGT-03 diagnosis behavior is implemented and regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_root_cause_agent.py` passes
- [ ] Root-cause outputs always include ranked evidence citations from retrieved cases
- [ ] Ambiguous evidence scenarios produce explicit `AMBIGUOUS` output
</verification>

<success_criteria>

- All tasks completed
- AGT-03 implemented with deterministic top-5 retrieval and structured evidence contract
- Root-cause output is explainable and directly consumable by remediator stage
- Tests protect retrieval and evidence formatting behavior
</success_criteria>

<output>
After completion, create `.planning/phases/03-root-cause-and-mcp-grounded-remediation/03-01-SUMMARY.md`
</output>

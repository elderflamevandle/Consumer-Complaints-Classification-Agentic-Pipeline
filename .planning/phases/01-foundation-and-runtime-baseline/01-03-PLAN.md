---
phase: 01-foundation-and-runtime-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/build_dataset.py
  - scripts/seed_vectordb.py
  - data/.gitkeep
  - data/README.md
  - src/tools/vector_index.py
  - tests/test_data_pipeline.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "CFPB narrative sample is reproducibly generated with fixed filtering and split rules."
    - "Vector index creation is deterministic and only rebuilds when dataset hash or model id changes."
  artifacts:
    - path: "scripts/build_dataset.py"
      provides: "10K stratified sample generation and split persistence"
      min_lines: 60
    - path: "scripts/seed_vectordb.py"
      provides: "Embedding and Chroma index seeding workflow"
      contains: "bge-large-en-v1.5"
    - path: "tests/test_data_pipeline.py"
      provides: "Sampling and stale-detection tests"
      min_lines: 35
    - path: "src/tools/vector_index.py"
      provides: "Manifest and stale-check helpers"
      contains: "dataset_hash"
  key_links:
    - from: "scripts/build_dataset.py"
      to: "scripts/seed_vectordb.py"
      via: "dataset artifact handoff"
      pattern: "parquet"
    - from: "scripts/seed_vectordb.py"
      to: "src/tools/vector_index.py"
      via: "manifest hash + stale logic"
      pattern: "stale|hash|model"
---

<objective>
Implement deterministic data preparation and vector seeding baseline for Phase 1.

Purpose: Deliver reproducible datasets and index artifacts required by future RAG-based phases.
Output: Sampling script, seeding pipeline, manifest helpers, and regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-and-runtime-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic CFPB sampling script with fixed split contract</name>
  <files>scripts/build_dataset.py, data/README.md, data/.gitkeep</files>
  <behavior>
    - Applies required filters (non-empty, min length, English-only).
    - Performs stratified sampling by product and issue.
    - Outputs 10K sample split as 7k/2k/1k with fixed seed metadata.
  </behavior>
  <action>Implement script that loads CFPB data, applies locked filters, performs stratified sampling, and writes parquet outputs and metadata documentation. Keep split logic deterministic and logged. Add `data/README.md` describing artifacts and rerun instructions.</action>
  <verify>
    <automated>uv run pytest -q tests/test_data_pipeline.py::test_sampling_split</automated>
  </verify>
  <done>Sampling pipeline produces reproducible outputs with documented split contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement vector seeding with hash-based stale detection</name>
  <files>scripts/seed_vectordb.py, src/tools/vector_index.py</files>
  <behavior>
    - Uses `bge-large-en-v1.5` embeddings for 5K subset.
    - Stores metadata fields: id, product, issue, state, date.
    - Rebuilds only when index missing or stale by dataset hash + model id.
    - On failure, returns warning-compatible status for fallback to prior index.
  </behavior>
  <action>Create vector seeding pipeline and helper utilities for manifest management. Persist index under local `chroma_db` plus version manifest. Implement stale detection exactly by dataset hash and model id, and expose helper return states for startup logic.</action>
  <verify>
    <automated>uv run pytest -q tests/test_data_pipeline.py::test_stale_detection</automated>
  </verify>
  <done>Index seeding is deterministic, metadata-rich, and refreshes only under defined stale conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add data pipeline regression tests and seed manifest checks</name>
  <files>tests/test_data_pipeline.py</files>
  <behavior>
    - Tests confirm split sizes and reproducibility.
    - Tests confirm stale detection and manifest write semantics.
    - Tests confirm startup-failure path can preserve prior index usage.
  </behavior>
  <action>Write targeted tests covering sampling determinism, hash-change triggers, model-id-change triggers, and manifest persistence. Include scenario tests validating continue-with-last-valid-index behavior contract.</action>
  <verify>
    <automated>uv run pytest -q tests/test_data_pipeline.py</automated>
  </verify>
  <done>Data and index contracts are regression-tested and ready for downstream phase integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_data_pipeline.py` passes
- [ ] Dataset metadata records seed and split details
- [ ] Seed manifest records dataset hash and embedding model ID
</verification>

<success_criteria>

- All tasks completed
- DATA-01 and DATA-02 fully covered
- Seeding behavior honors missing/stale-only rebuild rule
- Data artifacts are reproducible and documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-runtime-baseline/01-03-SUMMARY.md`
</output>

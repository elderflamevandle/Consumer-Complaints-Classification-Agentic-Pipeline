---
phase: 01-foundation-and-runtime-baseline
plan: "04"
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - scripts/tasks.py
  - Makefile
  - README.md
  - tests/test_task_runner.py
autonomous: true
gap_closure: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "A Windows PowerShell teammate can run the documented baseline commands without installing GNU Make."
    - "Operator command docs and task entrypoints are consistent across platforms."
  artifacts:
    - path: "scripts/tasks.py"
      provides: "Cross-platform command entrypoint for run/seed/eval/test/lint/typecheck"
      contains: "argparse"
    - path: "README.md"
      provides: "Cross-platform quickstart and operator command flow"
      contains: "uv run python scripts/tasks.py test"
    - path: "tests/test_task_runner.py"
      provides: "Regression coverage for task command dispatch"
      min_lines: 25
  key_links:
    - from: "README.md"
      to: "scripts/tasks.py"
      via: "documented primary task commands"
      pattern: "scripts/tasks.py"
    - from: "Makefile"
      to: "scripts/tasks.py"
      via: "optional Unix convenience wrapper"
      pattern: "scripts/tasks.py"
---

<objective>
Close the Phase 1 cross-platform operator-command gap found during UAT.

Purpose: Replace the GNU Make-only workflow with a first-class cross-platform task runner so Windows PowerShell users can execute the documented setup and quality gates directly.
Output: Cross-platform task CLI, updated docs, and regression coverage for command dispatch.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-runtime-baseline/01-UAT.md
@.planning/debug/windows-make-command-unavailable.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-platform task runner for baseline operator commands</name>
  <files>scripts/tasks.py, Makefile</files>
  <behavior>
    - Exposes `run`, `seed`, `eval`, `test`, `lint`, and `typecheck` commands without requiring GNU Make.
    - Executes the same underlying uv-backed commands already used by the repository.
    - Keeps `Makefile` as an optional wrapper rather than the only supported entrypoint.
  </behavior>
  <action>Implement a small Python task runner using `argparse` and `subprocess` that dispatches the baseline repository commands. Update the `Makefile` to delegate to that script so Unix users keep a short alias while Windows users have a first-class path.</action>
  <verify>
    <automated>uv run python scripts/tasks.py --help</automated>
  </verify>
  <done>Baseline tasks are reachable from a platform-neutral entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update README quickstart to make the cross-platform path primary</name>
  <files>README.md</files>
  <behavior>
    - Quickstart no longer assumes `make` exists.
    - Command flow lists the Python task runner first and clearly labels `make` as optional convenience.
    - Windows teammates can follow the primary documented path verbatim.
  </behavior>
  <action>Rewrite the quickstart and command-flow sections so the default setup path uses the new Python task runner. Keep the direct `uv run ...` equivalents or `make` aliases as secondary references only.</action>
  <verify>
    <automated>rg -n "scripts/tasks.py|make test|make lint|make typecheck" README.md</automated>
  </verify>
  <done>Repository documentation matches the new cross-platform operator contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for task dispatch and command contract</name>
  <files>tests/test_task_runner.py</files>
  <behavior>
    - Tests verify each supported subcommand resolves to the expected underlying command.
    - Tests cover invalid subcommands and exit behavior.
    - Coverage guards against future drift between docs and runnable command entrypoints.
  </behavior>
  <action>Create focused tests for the task runner command mapping, including success-path dispatch and unsupported-command behavior. Keep the tests fast and independent of external services.</action>
  <verify>
    <automated>uv run pytest -q tests/test_task_runner.py</automated>
  </verify>
  <done>Cross-platform command entrypoint is regression-tested.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python scripts/tasks.py test` works from the repo root
- [ ] `uv run python scripts/tasks.py lint` works from the repo root
- [ ] `uv run python scripts/tasks.py typecheck` works from the repo root
- [ ] `uv run pytest -q tests/test_task_runner.py` passes
</verification>

<success_criteria>

- OPS-01 remains satisfied on Windows PowerShell without extra GNU Make installation
- README quickstart and actual runnable commands match
- `Makefile` is optional convenience, not a hidden prerequisite
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-runtime-baseline/01-04-SUMMARY.md`
</output>

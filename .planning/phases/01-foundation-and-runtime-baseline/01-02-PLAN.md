---
phase: 01-foundation-and-runtime-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/llm/models.py
  - src/llm/client.py
  - src/llm/rate_limiter.py
  - src/schemas/classification.py
  - scripts/smoke_groq.py
  - tests/test_llm_client.py
autonomous: true
requirements:
  - AGT-01
user_setup:
  - service: groq
    why: "Live API smoke test requires key"
    env_vars:
      - name: GROQ_API_KEY
        source: "console.groq.com -> API Keys"
must_haves:
  truths:
    - "The LLM client retries transient failures and falls back through the locked model chain."
    - "Fallback trigger behavior is deterministic for 429, timeout, and transient 5xx errors."
  artifacts:
    - path: "src/llm/models.py"
      provides: "Model registry and fallback-chain constants"
      contains: "mixtral-8x7b-32768"
    - path: "src/llm/client.py"
      provides: "Groq client wrapper with retry/timeout/fallback"
      min_lines: 60
    - path: "tests/test_llm_client.py"
      provides: "Reliability policy regression tests"
      min_lines: 40
  key_links:
    - from: "src/llm/client.py"
      to: "src/llm/models.py"
      via: "model chain lookup"
      pattern: "FALLBACK|AGENT_MODELS"
    - from: "tests/test_llm_client.py"
      to: "src/llm/client.py"
      via: "policy behavior assertions"
      pattern: "fallback|retry|timeout"
---

<objective>
Implement the Groq reliability layer for deterministic retry, timeout, fallback, and budget-aware degradation.

Purpose: Satisfy AGT-01 before higher-level agents are built.
Output: Tested client wrapper + model registry + smoke script.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-and-runtime-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define model registry and fallback policy constants</name>
  <files>src/llm/models.py, src/schemas/classification.py</files>
  <behavior>
    - Chain includes `llama-3.3-70b-versatile -> mixtral-8x7b-32768 -> llama-3.1-8b-instant`.
    - Agent-to-model mapping is explicit and centrally defined.
  </behavior>
  <action>Implement model constants and helper accessors in `src/llm/models.py`. Keep fallback chain locked to user decision. Add minimal schema module used by client return types where needed. Do not hardcode unsupported model IDs.</action>
  <verify>
    <automated>uv run python -c "from src.llm.models import FALLBACK_CHAIN; assert FALLBACK_CHAIN[1]=='mixtral-8x7b-32768'"</automated>
  </verify>
  <done>Registry modules expose deterministic model routing and fallback constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Groq client with retry timeout and budget-degrade hooks</name>
  <files>src/llm/client.py, src/llm/rate_limiter.py</files>
  <behavior>
    - 3 retries with exponential backoff before model fallback.
    - Timeout enforced at 45s.
    - Fallback triggers on 429, timeout, transient 5xx.
    - Budget warning path can degrade non-critical 70B usage.
  </behavior>
  <action>Implement client wrapper around OpenAI-compatible Groq endpoint with deterministic error handling. Add rate/budget tracker helper with warning + degrade decision API. Keep logic isolated in llm package and avoid cross-module side effects.</action>
  <verify>
    <automated>uv run pytest -q tests/test_llm_client.py::test_retry_and_fallback</automated>
  </verify>
  <done>Client policy paths execute as specified with clear telemetry outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reliability tests and live smoke script</name>
  <files>tests/test_llm_client.py, scripts/smoke_groq.py</files>
  <behavior>
    - Unit tests validate fallback trigger classes.
    - Smoke script executes one structured completion when API key is present.
  </behavior>
  <action>Create focused tests for registry correctness, fallback ordering, retry count, timeout handling, and budget-degrade behavior. Add `scripts/smoke_groq.py` for optional live verification guarded by missing-key skip behavior.</action>
  <verify>
    <automated>uv run pytest -q tests/test_llm_client.py</automated>
  </verify>
  <done>Reliability module has regression coverage and a live smoke path for manual confidence checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_llm_client.py` passes
- [ ] Fallback chain contains required 3-model order
- [ ] Smoke script handles missing API key gracefully
</verification>

<success_criteria>

- All tasks completed
- AGT-01 behavior implemented and test-covered
- No unsupported fallback models remain in code
- Reliability policy is reusable for later agents
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-runtime-baseline/01-02-SUMMARY.md`
</output>

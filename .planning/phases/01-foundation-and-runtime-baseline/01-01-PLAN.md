---
phase: 01-foundation-and-runtime-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Makefile
  - .env.example
  - README.md
  - src/__init__.py
  - src/config.py
  - src/llm/__init__.py
  - src/agents/__init__.py
  - src/graph/__init__.py
  - src/tools/__init__.py
  - scripts/__init__.py
  - tests/conftest.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - OPS-01
user_setup:
  - service: groq
    why: "Live smoke check requires API key"
    env_vars:
      - name: GROQ_API_KEY
        source: "console.groq.com -> API Keys"
must_haves:
  truths:
    - "A teammate can bootstrap and run the project with documented commands."
    - "A modular repository skeleton exists for llm, agents, graph, tools, scripts, and tests."
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency and tooling configuration"
      contains: "[project]"
    - path: "Makefile"
      provides: "Standardized run/seed/eval/test commands"
      contains: "test:"
    - path: "README.md"
      provides: "Quickstart instructions"
      contains: "uv sync"
    - path: "tests/test_smoke.py"
      provides: "Bootstrap sanity test"
      min_lines: 10
  key_links:
    - from: "README.md"
      to: "Makefile"
      via: "documented command mapping"
      pattern: "make test"
    - from: "pyproject.toml"
      to: "tests/test_smoke.py"
      via: "pytest configuration"
      pattern: "pytest"
---

<objective>
Create deterministic project bootstrap scaffolding for Phase 1 execution.

Purpose: Ensure all downstream plans run on a stable, reproducible baseline.
Output: Repository structure, dependency/tool config, and smoke-testable quickstart.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-runtime-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-and-runtime-baseline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create repository skeleton and baseline config modules</name>
  <files>src/__init__.py, src/config.py, src/llm/__init__.py, src/agents/__init__.py, src/graph/__init__.py, src/tools/__init__.py, scripts/__init__.py</files>
  <behavior>
    - Importing `src.config` works without runtime errors.
    - Required package directories exist for future phase handoff.
  </behavior>
  <action>Create the modular directory skeleton exactly aligned with context decisions. Add minimal `__init__.py` markers and a `src/config.py` settings loader scaffold (env-aware but no feature logic). Keep scope strictly foundational; do not implement Phase 2+ behaviors.</action>
  <verify>
    <automated>uv run python -c "import src.config; print('ok')"</automated>
  </verify>
  <done>All required directories/files exist and config module imports successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dependency and quality-tooling baseline</name>
  <files>pyproject.toml, tests/conftest.py, tests/test_smoke.py</files>
  <behavior>
    - Test runner can discover and run smoke tests.
    - Ruff and mypy commands are defined in project config.
  </behavior>
  <action>Create `pyproject.toml` with Python 3.11+, uv-compatible dependency declarations, and tool sections for pytest/ruff/mypy-lite. Add `tests/conftest.py` and `tests/test_smoke.py` validating config import and core path sanity. Keep tests fast (<10s target).</action>
  <verify>
    <automated>uv run pytest -q tests/test_smoke.py</automated>
  </verify>
  <done>Tooling config and smoke tests execute cleanly via uv.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize operator commands and quickstart docs</name>
  <files>Makefile, .env.example, README.md</files>
  <action>Add Makefile targets: `run`, `seed`, `eval`, `test`, `lint`, `typecheck`. Create `.env.example` including `GROQ_API_KEY` placeholder and concise comments. Update README quickstart to include `uv sync`, `make test`, and expected command flow for Phase 1 outputs.</action>
  <verify>
    <automated>rg -n "^(run|seed|eval|test|lint|typecheck):" Makefile && rg -n "uv sync|make test" README.md</automated>
  </verify>
  <done>A new teammate can follow README + Makefile and run baseline checks without ad hoc steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_smoke.py` passes
- [ ] `uv run ruff check .` passes
- [ ] `uv run mypy src` passes (lightweight config)
</verification>

<success_criteria>

- All tasks completed
- Bootstrap scaffolding exists and is import/test verified
- README + Makefile provide a reproducible setup path
- No Phase 2+ capability work included
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-runtime-baseline/01-01-SUMMARY.md`
</output>

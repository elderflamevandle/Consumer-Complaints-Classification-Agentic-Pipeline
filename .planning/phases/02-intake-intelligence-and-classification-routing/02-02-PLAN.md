---
phase: 02-intake-intelligence-and-classification-routing
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/agents/classifier.py
  - src/schemas/classification.py
  - tests/test_classifier_agent.py
autonomous: true
requirements:
  - AGT-02
must_haves:
  truths:
    - "Classifier returns strict JSON with required AGT-02 fields."
    - "Invalid schema output triggers bounded repair retries and fallback behavior."
    - "Missing confidence values resolve through deterministic heuristic path."
  artifacts:
    - path: "src/agents/classifier.py"
      provides: "Schema-enforced classifier wrapper with retry/repair logic"
      min_lines: 70
    - path: "src/schemas/classification.py"
      provides: "Constrained enums + OTHER taxonomy contract"
      contains: "OTHER"
    - path: "tests/test_classifier_agent.py"
      provides: "AGT-02 contract and retry policy regression tests"
      min_lines: 45
  key_links:
    - from: "src/agents/classifier.py"
      to: "src/llm/client.py"
      via: "shared Groq reliability path"
      pattern: "complete|fallback|retry"
    - from: "tests/test_classifier_agent.py"
      to: "src/agents/classifier.py"
      via: "schema and heuristic assertions"
      pattern: "strict|retry|heuristic|confidence"
---

<objective>
Implement AGT-02 classifier contract with strict schema validation, repair retries, and deterministic confidence fallback handling.

Purpose: ensure all downstream routing receives valid, typed classification outputs.
Output: classifier service + tests enforcing schema and recovery policies.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-CONTEXT.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-RESEARCH.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend classification schema with constrained taxonomy contract</name>
  <files>src/schemas/classification.py</files>
  <behavior>
    - Required fields remain strict and validated.
    - `product_type` and `issue_type` support constrained enums with explicit `OTHER` option.
    - Confidence remains bounded to [0, 1].
  </behavior>
  <action>Update schema definitions and helper validators to align with AGT-02 decisions while preserving compatibility with downstream routing and explainability layers.</action>
  <verify>
    <automated>uv run pytest -q tests/test_classifier_agent.py::test_classifier_strict_schema</automated>
  </verify>
  <done>Schema module fully represents locked AGT-02 output contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build classifier agent wrapper with repair retries and fallback integration</name>
  <files>src/agents/classifier.py</files>
  <behavior>
    - Prompts for strict JSON output only.
    - On invalid output: run at most 2 repair retries before fallback chain progression.
    - Missing/invalid confidence uses keyword-score heuristic fallback.
  </behavior>
  <action>Implement classifier orchestration around shared LLM client, including structured parse/validate flow, retry budget handling, and deterministic fallback object creation for edge failures.</action>
  <verify>
    <automated>uv run pytest -q tests/test_classifier_agent.py::test_invalid_json_repair_retry_then_fallback</automated>
  </verify>
  <done>Classifier behavior is deterministic and AGT-02 compliant under normal and failure scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add AGT-02 regression tests</name>
  <files>tests/test_classifier_agent.py</files>
  <behavior>
    - Tests validate strict JSON parse contract.
    - Tests validate repair retries and fallback delegation.
    - Tests validate keyword-score heuristic confidence behavior.
  </behavior>
  <action>Create focused policy tests with transport mocks/stubs to keep runtime fast and deterministic.</action>
  <verify>
    <automated>uv run pytest -q tests/test_classifier_agent.py</automated>
  </verify>
  <done>AGT-02 contract has repeatable automated verification coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_classifier_agent.py` passes
- [ ] AGT-02 required fields always present in successful classifier outputs
- [ ] Retry/repair budget and heuristic confidence paths are test-covered
</verification>

<success_criteria>

- All tasks completed
- AGT-02 implemented with strict schema and recovery policy
- Classifier output is stable and machine-consumable for routing
- Tests guard schema drift and formatting regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-intelligence-and-classification-routing/02-02-SUMMARY.md`
</output>
---
phase: 02-intake-intelligence-and-classification-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intake/__init__.py
  - src/intake/pii.py
  - src/intake/receipt_merge.py
  - src/intake/pipeline.py
  - src/schemas/intake.py
  - tests/test_intake_pipeline.py
autonomous: true
requirements:
  - DATA-04
  - FLOW-01
must_haves:
  truths:
    - "Complaint text is scrubbed before any LLM-bound classification call."
    - "Receipt text can be appended deterministically as optional intake context."
    - "Low-confidence scrub path follows reviewer-available vs unavailable policy."
  artifacts:
    - path: "src/intake/pii.py"
      provides: "Typed-token PII scrubbing and confidence scoring"
      contains: "[NAME]"
    - path: "src/intake/pipeline.py"
      provides: "Pre-LLM intake preparation contract"
      min_lines: 60
    - path: "tests/test_intake_pipeline.py"
      provides: "FLOW-01 and DATA-04 policy regressions"
      min_lines: 40
  key_links:
    - from: "src/intake/pipeline.py"
      to: "src/intake/pii.py"
      via: "scrub-first call path"
      pattern: "scrub|sanitize"
    - from: "tests/test_intake_pipeline.py"
      to: "src/intake/pipeline.py"
      via: "pre-LLM policy assertions"
      pattern: "before_llm|receipt|low_confidence"
---

<objective>
Implement intake preprocessing baseline for Phase 2: typed PII scrubbing, optional receipt merge, and scrub-failure policy handling before classification.

Purpose: satisfy DATA-04 + FLOW-01 with deterministic behavior and test coverage.
Output: intake pipeline module + tests proving no unsanitized text reaches classifier path.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-CONTEXT.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-RESEARCH.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build typed-token PII scrubber with confidence metadata</name>
  <files>src/intake/__init__.py, src/intake/pii.py, src/schemas/intake.py</files>
  <behavior>
    - Replaces detected entities with typed placeholders (`[NAME]`, `[PHONE]`, `[EMAIL]`, etc.).
    - Returns scrubbed text and scrub-confidence score.
    - Preserves non-PII complaint semantics where possible.
  </behavior>
  <action>Implement deterministic scrubber utilities and intake payload schema containing raw text, scrubbed text, confidence score, warning flags, and trace fields required for downstream routing.</action>
  <verify>
    <automated>uv run pytest -q tests/test_intake_pipeline.py::test_pii_scrub_before_llm</automated>
  </verify>
  <done>PII scrubbing contract exists and can be consumed by intake pipeline and routing nodes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement optional receipt merge and pre-LLM intake preparation</name>
  <files>src/intake/receipt_merge.py, src/intake/pipeline.py</files>
  <behavior>
    - Optional receipt text append occurs before classification payload finalization.
    - Reviewer-available + scrub-low-confidence routes to human review gate.
    - Reviewer-unavailable + scrub-low-confidence continues with warning/high-risk flag.
  </behavior>
  <action>Create deterministic intake preparation function that normalizes complaint text, invokes scrubber first, merges optional receipt context, and emits routing hints consumed by later plans.</action>
  <verify>
    <automated>uv run pytest -q tests/test_intake_pipeline.py::test_receipt_merge_contract</automated>
  </verify>
  <done>FLOW-01 and DATA-04 behavior implemented as explicit policy logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add intake pipeline regression tests</name>
  <files>tests/test_intake_pipeline.py</files>
  <behavior>
    - Tests confirm scrub-before-LLM contract.
    - Tests confirm optional receipt merge placement.
    - Tests confirm low-confidence scrub policy split by reviewer availability.
  </behavior>
  <action>Write focused tests covering positive path and boundary/error-handling conditions, with deterministic assertions suitable for CI and local development.</action>
  <verify>
    <automated>uv run pytest -q tests/test_intake_pipeline.py</automated>
  </verify>
  <done>Intake safety and receipt merge behavior are regression protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_intake_pipeline.py` passes
- [ ] Intake preparation never sends raw text to classifier-bound payload field
- [ ] Reviewer available/unavailable low-confidence policy is test-covered
</verification>

<success_criteria>

- All tasks completed
- DATA-04 and FLOW-01 implemented with deterministic contracts
- Scrubber and receipt merge are reusable by classifier and routing layers
- Tests capture policy boundaries and failure handling
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-intelligence-and-classification-routing/02-01-SUMMARY.md`
</output>
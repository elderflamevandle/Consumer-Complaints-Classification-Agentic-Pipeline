---
phase: 02-intake-intelligence-and-classification-routing
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/graph/state.py
  - src/graph/routing.py
  - src/graph/interrupts.py
  - tests/test_routing_interrupts.py
autonomous: true
requirements:
  - FLOW-02
must_haves:
  truths:
    - "Routing sends low-confidence OR high-risk cases to human interrupt path."
    - "Reviewer actions approve/edit/reject are represented and handled explicitly."
    - "Post-review continuation preserves same-thread execution context."
  artifacts:
    - path: "src/graph/routing.py"
      provides: "Deterministic FLOW-02 routing policy"
      min_lines: 50
    - path: "src/graph/interrupts.py"
      provides: "Human-review interrupt payload/action handlers"
      contains: "approve"
    - path: "tests/test_routing_interrupts.py"
      provides: "FLOW-02 decision and resume behavior tests"
      min_lines: 40
  key_links:
    - from: "src/graph/routing.py"
      to: "src/agents/classifier.py"
      via: "confidence/risk based branching"
      pattern: "confidence|compliance_risk"
    - from: "tests/test_routing_interrupts.py"
      to: "src/graph/interrupts.py"
      via: "review action and thread resume assertions"
      pattern: "approve|edit|reject|thread"
---

<objective>
Implement FLOW-02 routing and interrupt/resume behavior for low-confidence or high-risk classifications.

Purpose: provide deterministic human-in-the-loop branch control before later multi-agent phases.
Output: routing policy + interrupt contract + regression tests.
</objective>

<execution_context>
@C:/Users/Chaitanya/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Chaitanya/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-CONTEXT.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-RESEARCH.md
@.planning/phases/02-intake-intelligence-and-classification-routing/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic FLOW-02 routing policy</name>
  <files>src/graph/routing.py, src/graph/state.py</files>
  <behavior>
    - Route to human review when confidence < 0.70 OR compliance risk is high/critical.
    - Non-interrupt cases continue to next pipeline stage.
    - Routing function is deterministic and unit-testable.
  </behavior>
  <action>Create policy functions and typed state models for routing decisions that consume classifier + intake metadata and produce explicit route outcomes.</action>
  <verify>
    <automated>uv run pytest -q tests/test_routing_interrupts.py::test_route_low_confidence_or_high_risk</automated>
  </verify>
  <done>FLOW-02 branching behavior exists as explicit code contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add interrupt payload and reviewer action handlers</name>
  <files>src/graph/interrupts.py</files>
  <behavior>
    - Interrupt payload includes reviewer action options: approve/edit/reject.
    - Reviewer response updates thread state without resetting thread identity.
    - Reviewer edits are captured for downstream continuation.
  </behavior>
  <action>Implement interrupt payload schema and resume handlers that preserve same-thread continuity and produce deterministic state transitions.</action>
  <verify>
    <automated>uv run pytest -q tests/test_routing_interrupts.py::test_reviewer_actions_approve_edit_reject</automated>
  </verify>
  <done>Human-review path is explicit, testable, and resume-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add routing and resume regression tests</name>
  <files>tests/test_routing_interrupts.py</files>
  <behavior>
    - Tests validate low-confidence/high-risk interrupt criteria.
    - Tests validate same-thread resume semantics after review.
    - Tests validate approve/edit/reject action handling.
  </behavior>
  <action>Write targeted tests with deterministic state fixtures and boundary values around the 0.70 threshold.</action>
  <verify>
    <automated>uv run pytest -q tests/test_routing_interrupts.py</automated>
  </verify>
  <done>FLOW-02 routing behavior is regression protected.
</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_routing_interrupts.py` passes
- [ ] Interrupt trigger rules exactly match 0.70 + high-risk policy
- [ ] Approve/edit/reject actions preserve same-thread continuation
</verification>

<success_criteria>

- All tasks completed
- FLOW-02 implemented with deterministic interrupt rules
- Reviewer action semantics are explicit and tested
- Routing layer is ready for downstream root-cause/remediation phases
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-intelligence-and-classification-routing/02-03-SUMMARY.md`
</output>